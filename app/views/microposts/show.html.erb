<p id="notice"><%= notice %></p>


<p>
  <strong>Title:</strong>
  <%= @micropost.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @micropost.content %>
</p>

<p>
  <strong>User:</strong>
  <%= @micropost.user_id %>
</p>

<h2>Comments</h2>
<% @micropost.comments.each do |comment| %>
  <p>
    <strong>Comment:</strong>
    <%= comment.body %>
  
    <font color="blue">User :</font>
    <%= comment.user.name %>
    
    <font color="green">Creat fa :</font>
    <%= time_ago_in_words(comment.created_at) %>
    
    <button type="button" class="btn btn-danger"><%= link_to 'Delete',comment, method: :delete, data: { confirm: 'Estàs segur?' }%></button>&nbsp;
    
  </p>
<% end %>

<h2>Add a comment:</h2>
<%= form_for([@micropost, @micropost.comments.new]) do |f| %>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Edit', edit_micropost_path(@micropost) %> |
<%= link_to 'Back', microposts_path %>
